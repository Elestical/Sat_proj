# secrets — never commit
kaggle.json
.kaggle/
*.pem
*.key
.env

# data + heavy artifacts (fetched/generated at runtime)
data/deepglobe/
data/patches/
*.tif
*.npz
outputs/*.pt

# generated products (rebuilt by the scripts; the result figures are kept deliberately)
outputs/dashboard/
outputs/cells.geojson
outputs/forecast_cells.csv

# python / editor / os cruft
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/
.DS_Store
